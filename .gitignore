# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/

# Recorded frames and debug overlays (100+ MB of raw depth). data/scans/ - the pipeline's annotated pictures
# and order.json - is tracked, so runs on one laptop can be looked at from another.
data/frames/
data/debug/

# Downloaded model weights (the YOLOE text encoder alone is 600 MB)
models/

# OS / editor
.DS_Store
.idea/
.vscode/
